edition = "2021"
max_width = 100
use_small_heuristics = "Max"
reorder_imports = true

# Note: `imports_granularity = "Module"` and `group_imports = "StdExternalCrate"`
# are nightly-only rustfmt options. They were dropped to keep formatting
# consistent across the stable toolchain pinned in `rust-toolchain.toml`.
