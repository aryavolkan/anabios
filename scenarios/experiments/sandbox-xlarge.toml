name = "sandbox-xlarge"
seed = 11
world_size = 4096.0
biome_res = 512
hash_res = 256
living_biome = true
season_period = 4000
max_population = 12000

# X-large sandbox: the 12k-cap stress world at 4096² (biome 512²,
# seasons). Exists to prove determinism and throughput at the top of the
# population budget.

[[agents]]
count = 4000
placement = { kind = "uniform" }
archetype = "asocial_forager"

[[agents]]
count = 500
placement = { kind = "uniform" }
archetype = "skilled_forager"
